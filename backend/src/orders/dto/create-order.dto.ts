// CreateOrderDto is intentionally empty as orders are created from the user's cart
// The order creation process reads from the cart automatically
export class CreateOrderDto {
  // No fields needed - order is created from current cart contents
}